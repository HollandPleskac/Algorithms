''' Task 1 Initialize Variables : initialize the following variables
 * number_to_find : a variable to store the value being searched for (24 lbs.)
 * min_index : a variable to store the bottom end of the searchable range.
 * max_index : a variable to store the top end of the searchable range.
 * l : the list being searched
'''

number_to_find = 24
1 = [6, 20, 21, 24, 30, 32, 80, 85, 90, 101]
min_dinex = 0
max_index = len(l)-1
